# Future-phase drafts (NOT part of Phase 1)

These migrations and Edge Functions were drafted for the full-parity build
(extended operations schema, transactional checkout, Square webhook,
notifications). They are parked here untouched while Phase 1 establishes the
approved data foundation with its own migration set in ../migrations/.
They will be rebased onto the Phase 1 schema in later approved phases.
Do not run these against the Phase 1 database.
